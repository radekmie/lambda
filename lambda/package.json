{
  "name": "lambda",
  "private": true,
  "scripts": {
    "build": "parcel build index.html --detailed-report --no-autoinstall --no-content-hash --no-source-maps --out-dir ../docs --public-url https://radekmie.github.io/lambda/",
    "start": "parcel watch index.html"
  },
  "dependencies": {
    "dagre-d3": "0.6.3",
    "graphlib-dot": "0.6.2"
  },
  "devDependencies": {
    "parcel-bundler": "1.12.3"
  }
}
